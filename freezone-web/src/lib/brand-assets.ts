const BRAND_ASSET_VERSION = "7";

/** FreeZone maroon Z mark — sidebar, header, language toggle, avatar fallback */
export const FREEZONE_Z_LOGO = `/brand/freezone-z-logo.png?v=${BRAND_ASSET_VERSION}`;

const FAVICON_VERSION = BRAND_ASSET_VERSION;

/** Browser tab icon (favicon.ico is what Chrome fetches first). */
export const FREEZONE_FAVICON = `/favicon.ico?v=${FAVICON_VERSION}`;
