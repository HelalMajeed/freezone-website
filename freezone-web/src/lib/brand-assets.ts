/** FreeZone maroon Z mark — sidebar, avatar fallback, favicon */
export const FREEZONE_Z_LOGO = "/brand/freezone-z-logo.png";

const FAVICON_VERSION = "5";

/** Browser tab icon (favicon.ico is what Chrome fetches first). */
export const FREEZONE_FAVICON = `/favicon.ico?v=${FAVICON_VERSION}`;
