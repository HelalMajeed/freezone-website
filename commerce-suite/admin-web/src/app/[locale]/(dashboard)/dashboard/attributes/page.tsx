export default function AttributesPlaceholderPage() {
  return (
    <div className="rounded-xl border border-dashed border-border bg-white p-8 text-center text-sm text-muted">
      Attribute builder module — next iteration (CRUD + category assignment UI already supported server-side via{" "}
      <code className="rounded bg-surface px-1">/attributes</code>).
    </div>
  );
}
