# Claude Code session state — FreeZone overhaul

> Auto-maintained file. New Claude Code sessions should read this first, then
> the latest `ProgressState` row in the production DB (or `gh issue list
> --label sprint-report` if DB is unreachable), then resume from the next
> incomplete sprint.

## Project context

- Repo: <https://github.com/HelalMajeed/freezone-website>
- Storefront: <https://freezone-iq.com/en>
- Admin: <https://freezone-iq.com/admin>
- API: <https://freezone-website.fly.dev>
- Source for import: <https://globaliraq.iq/ar> (Shopify, 1231 products)

## Operating rules

- Direct push to `main` for small/safe fixes only.
- Larger work goes on a `claude/<sprint-or-topic>` branch + PR.
- Long-running work (>30min wall time) belongs in GitHub Actions or Fly Machine
  workers, not the local session.
- Never wait for owner approval. Instead, open a GitHub issue:
  - `workaround` + `needs-review` — non-ideal solution applied.
  - `assumption` + `needs-review` — decision made without explicit approval.
  - `blocker` + `urgent` — true hard stop (missing credentials, etc.).
  - `sprint-report` — at end of each sprint.
  - `daily-summary` — auto-posted by the daily workflow.
- Red lines: no destructive DB ops (`reset`, `--force-reset`, `TRUNCATE`,
  `DELETE FROM` on prod tables), no secrets in code/logs/PRs, no Shopify CDN
  or `globaliraq.iq` URLs as final image URLs, no slugs with timestamps, no
  half-finished `✅` reports, no force-push to `main`.

## Sprint plan (7 sprints + continuous mode)

0. Infrastructure — `ProgressState`, labels, workflows, run-batch endpoint, Sentry stub.
1. Foundation — `resolveCategory`, `ensureCategoryAttributes`, atomic stock decrement, indexes, switch importer back to HTTP Plan A.
2. Admin API/UX — pagination, bulk actions, rate limiting, CORS allow-list, helmet, zod.
3. Variants + warranty — `parseAndDoubleWarranty` with unit tests, full Shopify variants persisted + rendered, sharp srcset, auto-publish gates.
4. UX/i18n — 404, error boundary, alert/confirm sweep, RTL polish.
5. SEO + a11y + perf — sitemap.xml, robots.txt, OG/JSON-LD, hreflang, Lighthouse 90+.
6. Tests + CI — vitest on freezone-api, playwright e2e.
7. Import launch — manual 50-product batch → 90 %+ pass → enable cron → run to completion.
8. Continuous (after launch) — watch-mode workflow polls sitemap for new handles.

## Where we are now

The cell below is updated by the latest run.

```yaml
current_sprint: ship-launch
current_step: SHIPPED-AND-LIVE
last_session_at: 2026-06-14T19:30Z
last_completed_sprint: ship-launch   # feat/global-launch merged to main + deployed live
resume_run_2026_06_14: >
  Reconcile-and-finish run. Verified (code + live, trust-code-not-claims) the launch is
  genuinely complete and green on main (c18e395). All SHIP Phase-2 areas VERIFIED-DONE
  on the live site. Fixed the one real gap: imported-vendor Brand rows — added
  create-imported-brands action to ops-fly.yml (33997fa) + machine-wake fix (46e8883),
  owner-authorized, ran on prod (ops run 27509566996): +115 brand rows, live brand count
  14 -> 129. Lighthouse not measurable from session env (no local LH, PSI quota out) ->
  follow-up issue #46 (no number fabricated). #44 backups still owner-blocked. Full
  evidence in RESUME_STATUS.md.
resume_run_2026_06_15: >
  Deep re-verification (trust-code-not-claims). Re-ran build gates locally (API 238/238 +
  build; web 32/32 + lint + build, all PASS) and re-checked live (catalog 1506 / 129 brands,
  product SEO+JSON-LD, sitemap 3328 urls, robots, admin 401 guard, /admin noindex).
  Fresh owner-authorized COD round-trip on prod: FZ-00007 placed -> tracked -> cancelled via
  admin (ops run 27513795761, stock restored) -> re-tracked cancelled; no fake order left.
  NEW gaps found + filed: #48 soft-404 (unknown slugs return HTTP 200) + long-tail products
  lack server meta (only top-500/locale prerendered) - risky to fix on live, owner decision.
  Lighthouse still unmeasured (#46, PSI quota). No app code changed; live stays on c18e395.
notes: >
  SHIPPED 2026-06-11. feat/global-launch merged to main (3c258be) and deployed;
  deploy-production green. Read SHIP_REPORT.md + SHIP_STATUS.md.
  Live: storefront freezone-iq.com (ar/en, sitemap XML, security headers, JSON-LD),
  admin /admin (credentialed, SUPER_ADMIN admin@freezone-iq.com seeded), API on Fly.
  Full globaliraq import done: 1421 products imported (0 dupes), 1112 published,
  1509 total. COD order round-trip verified on prod (placed FZ-00006 -> tracked ->
  cancelled via admin). classification:repair run.
  IMPORTANT ops changes this ship:
   - Fly machine scaled 1GB -> 2GB (fly.toml) + Dockerfile node --max-old-space-size=1536.
   - Catalog OOM fixed: getProductsCatalog now uses LEAN include (no EAV) + a
     single-flight/SWR cache (src/lib/ttl-cache.ts). The bulk bootstrap no longer
     loads attributeValues; storefront facet filtering uses the paginated
     /api/ssr/catalog/products endpoint; PDP loads rich EAV per-id.
   - Importer hardened: sitemap.ts fetch timeouts, ssrf-url dns.lookup timeout,
     120s per-product ceiling in run-batch.ts.
   - import-globaliraq.yml now logs in via /api/dashboard/auth/login (legacy
     /api/admin/login cookie is mutation-blocked in prod). New import-globaliraq-full.yml
     loops batches. ops-fly.yml = Fly admin ops (secrets/seed/classification/cancel-test-order).
  OPEN: issue #44 (DATABASE_URL_PROD backups are a no-op; Fly volume snapshots are
  the only DR path). ~309 imported products still DRAFT (needs triage). Brand rows
  not auto-created for imported vendors.
```

## How to resume

1. Read this file (you are here).
2. `git fetch origin && git log --oneline origin/main -5`
3. `gh issue list --label sprint-report --limit 5`
4. `psql $DATABASE_URL -c 'SELECT * FROM "ProgressState" ORDER BY "updatedAt" DESC LIMIT 10;'`
   — only if DB connection is set up.
5. Pick the next sprint that is not yet `completed`.
