# Base URL for commerce-admin-api (Nest global prefix is `v1`).
# Admin routes: GET/PATCH /v1/admin/sub-services, PATCH /v1/admin/sub-services/reorder/batch
# Public: GET /v1/offers
VITE_API_BASE_URL=http://127.0.0.1:3020/v1
