/**
 * Per-category filter + display spec schemas (source of truth for classification presets).
 * @see category-filter-schema.ts
 */
export {
  CLASSIFICATION_SEED_BY_SLUG,
  CATEGORY_SCHEMA_SLUG_ALIASES,
  getCategoryFilterSchema,
  resolveCategorySchemaSlug,
} from "./category-filter-schema";
